CREATE TABLE signup (
    date TEXT NOT NULL,
    name TEXT NOT NULL,
    signup_ts TEXT NOT NULL,
    cancel INTEGER,
    PRIMARY KEY (date, name)
);

CREATE TABLE signup_nameonly (
    name TEXT NOT NULL
);